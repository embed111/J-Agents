# 执行提示词-工程化重构四轮结构契约化-20260304

你现在在 `../workflow` 工作区执行“工程化重构第四轮（结构契约化）”。  
本轮重点不是新增功能，而是让目录结构对 agent 更可维护、更可接管。

## 1. 先读文件（必做）
1. `docs/workflow/requirements/需求详情-工程化重构与目录治理.md`
2. `docs/workflow/design/详细设计-工程化重构与目录治理.md`
3. `docs/workflow/reports/ARCHITECTURE_REFACTOR_MAP.md`
4. `docs/workflow/reports/REFactor_LINE_BUDGET_REPORT.md`

## 2. 本轮核心目标（必须全部命中）
1. 完成后端模块 README 契约化：
   1. `src/workflow_app/server/README.md`
   2. `src/workflow_app/server/api/README.md`
   3. `src/workflow_app/server/services/README.md`
   4. `src/workflow_app/server/infra/README.md`
   5. `src/workflow_app/server/presentation/README.md`
   6. `src/workflow_app/server/bootstrap/README.md`
2. 完成前端结构契约化：
   1. 为 `src/workflow_app/web_client/README.md` 增补“去序号命名迁移计划 + 模块边界 + 组装机制说明”。
3. 去原型化文本清理（运行时源码范围）：
   1. 清理 `src/workflow_app/**/*.py` 与 `src/workflow_app/web_client/**/*.js` 中 `Phase0/Phase 0/Phase1/Phase 1` 文案。
4. 增加结构门禁脚本（至少 2 个）：
   1. `scripts/check-module-readme-contract.py`：检查模块 README 必填段。
   2. `scripts/check-prototype-text.py`：检查运行时代码无 Phase 文案。
5. UO + AR 验收脚本继续通过。

## 3. README 契约必填段（缺一即 fail）
1. 模块目标与非目标
2. 目录职责边界（In/Out）
3. 对外接口（API/Event/函数入口）
4. 允许依赖与禁止依赖
5. 状态与数据存储（表/文件/缓存）
6. 关键回归命令
7. 常见变更操作步骤（新增接口/改字段）
8. 回滚策略
9. Agent 接管入口（先读文件、执行顺序、最小验证）

## 4. 第四轮硬门禁（任一失败即 `fail_hard_gate`）
1. 结构门禁：
   1. 上述 6 个后端目录 README 全部存在且通过契约检查脚本。
2. 文本门禁：
   1. `src/workflow_app/` 运行时代码中 `Phase0/Phase 0/Phase1/Phase 1` 搜索结果为空。
3. 体量回归门禁（防回流）：
   1. `src/workflow_app/server/bootstrap/web_server_runtime.py <= 3000`
   2. `src/workflow_app/server/api/legacy_route_handlers.py <= 1000`
4. 验收门禁：
   1. AR/UO 脚本均 `exit code 0`。

## 5. 禁止事项
1. 禁止夹带新功能需求。
2. 禁止只补 README 标题不补可执行内容。
3. 禁止只回传门禁统计截图，缺少功能截图/API/日志/代码路径。
4. 禁止把 README 写成空泛描述，必须能指导 agent 执行。

## 6. 必交证据
1. `README_CONTRACT_REPORT.md`（逐目录契约检查结果）。
2. `PROTOTYPE_TEXT_SCAN_REPORT.txt`（源码 Phase 文案扫描结果，必须为空）。
3. `hard_gate_v4_counts.txt`（关键文件行数）。
4. AC 证据：每条 AC 都要有功能截图 + API/脚本 + 日志/DB + 代码路径。
5. AR/UO 回归结果与退出码。

## 7. 回传格式（固定）
1. 变更摘要（文件列表 + 目的）。
2. AC 清单（`AC-ID | pass/fail | evidence`）。
3. 第四轮硬门禁结果（`gate-id | pass/fail | evidence`）。
4. 风险与未完成项（若有）。
5. 下一步建议（最多 3 条）。


