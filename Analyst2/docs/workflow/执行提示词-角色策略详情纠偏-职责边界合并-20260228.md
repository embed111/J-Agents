# 执行提示词：角色策略详情纠偏（职责边界合并）- 2026-02-28

你在 `../workflow` 工作区执行本任务。本轮是纠偏修复，不是新功能扩展。

## 1. 当前未通过点（先修这两条）
1. 已做：`角色设定门禁得分项` 默认折叠。
2. 未做：页面仍是“`角色/职责/目标` + `限制内容`”双模块，未合并为“`角色/目标` + `职责边界`”。

## 2. 修复目标（必须全部满足）
1. 首屏结构改为：
   1. `角色/目标`（只包含角色与目标）
   2. `职责边界`（统一承载 must/must_not/preconditions）
   3. `角色设定门禁得分项`（默认折叠）
2. `门禁与来源`、`AGENTS.md 原文证据`继续默认折叠。
3. 不允许在首屏继续出现独立“限制内容”模块。
4. 不允许在“角色/目标”模块里继续出现单独“职责”卡片。

## 3. 必读文档
1. `docs/workflow/需求概述.md`（第 13 章）
2. `docs/workflow/需求详情-训练闭环监控系统-Phase0.md`（FR-31、FR-32、FR-37、AC-36、AC-37、AC-43）

## 4. 实施要点（按文件改）

### 4.1 `scripts/day3_web_app.js`
1. 在策略确认弹窗渲染逻辑中：
   1. 将 `角色/职责/目标` 改为 `角色/目标`。
   2. 删除 core 模块中的“职责”卡片渲染。
   3. 将“限制内容（must / must_not / preconditions）”模块改名为“职责边界（must / must_not / preconditions）”。
   4. 职责边界模块保留三分类与证据展示，不降级功能。
2. 在复制文本 `policyConfirmCopyText()` 中同步更新布局描述：
   - 由“角色/职责/目标 -> 限制内容 -> 门禁得分项”
   - 改为“角色/目标 -> 职责边界 -> 门禁得分项”
3. 保持 `score` 模块 `collapsible: true` 且 `expanded: false`（默认折叠）。

### 4.2 `scripts/day3_web_chat.py`
1. 保留 `constraints` 结构字段（兼容旧载荷），但对外文案统一为“职责边界”语义。
2. 与 UI 展示一致：不再引导成“职责 + 限制内容”双模块。
3. 评分修复建议文案若写“限制内容”，改为“职责边界（must/must_not/preconditions）”。

## 5. 验收（必须逐条给证据）
1. AC-36：首屏先看到 `角色/目标`，随后 `职责边界`，再是 `门禁得分项`。
2. AC-37：`门禁得分项`、`门禁与来源`、`原文证据`默认折叠。
3. AC-43：`职责边界`中能看到 `must/must_not/preconditions` 三类与证据映射，且边界冲突/缺证据时门禁非 `auto`。
4. 额外强校验：
   1. 首屏 DOM 中不再出现模块标题“限制内容（must / must_not / preconditions）”。
   2. 首屏 DOM 中不再出现模块标题“角色/职责/目标”。

## 6. 回传格式
1. 改动文件 + 函数列表。
2. 关键 diff（贴出模块标题和渲染顺序相关代码片段）。
3. 3 张截图：
   1. 首屏模块顺序
   2. 默认折叠状态
   3. 职责边界三分类与证据
4. AC-36/AC-37/AC-43 pass/fail 与证据路径。
