# 历史归档

- 归档时间: 2026-03-06 12:04:35
- 来源文件: workspace_state/core/session-snapshot.md
- 覆盖区间: 2026-03-02 ~ 2026-03-05
- 归档块数: 48

## 本轮更新（2026-03-02）- Skill固化必要性复核
1. 用户问题（明示）：
   - 当前提示词治理流程是否需要固化为 skill。
2. 当前判断：
   - 已满足固化条件（重复执行、步骤稳定、门禁明确），建议创建 1 个轻量 skill 统一执行。
3. 建议边界：
   - 仅固化“提示词资产治理与分发顺序”流程，不扩展到实现验收自动化。

## 本轮更新（2026-03-02）- 角色卡矢量图语义澄清
1. 用户澄清（明示）：
   - “矢量图”应理解为头像式图标（名片/工牌/身份证风格），每个 agent 像真实工作角色。
2. 已执行动作：
   - 更新 `docs/workflow/需求概述.md` 第 15 节入口展示样式。
   - 更新 `docs/workflow/需求详情-统一入口与训练优化模块.md`（核心场景、FR-UO-02、AC-UO-01）。
   - 更新 `docs/workflow/详细设计-统一入口与训练优化模块.md`（角色卡字段语义、视觉验收补充）。
   - 更新 `docs/workflow/prompts/执行提示词-统一入口与训练优化模块-20260302.md`（实现边界新增视觉语义约束）。
   - 已同步至 `../workflow` 并完成哈希一致性校验。

## 本轮更新（2026-03-02）- `../workflow` 开工触发指引
1. 用户问题（明示）：
   - 现在如何让 `../workflow` 开始工作。
2. 指引口径：
   - 先确认执行文档已在 `../workflow/docs/workflow/prompts/`。
   - 再向执行方下发固定口令触发开工。
3. 固定口令：
   - `请按照文件《docs/workflow/prompts/执行提示词-统一入口与训练优化模块-20260302.md》中的提示词执行。`

## 本轮更新（2026-03-03）- `../workflow` 交付验收复核
1. 用户请求（明示）：
   - “他交付了，验收一下试试”。
2. 已执行动作（事实）：
   - 运行验收脚本：`python ../workflow/scripts/acceptance/run_acceptance_training_center_uo.py --root ../workflow --port 8126`。
   - 脚本返回码：`0`。
   - 读取最新证据：`C:/work/agents/workflow/.output/evidence/training-center-uo-20260303-112401/` 下 `ac_uo_summary.md`、`ac_uo_12_evidence_matrix.json`、关键截图/API/DB 证据。
   - 人工补验角色卡视觉语义：头像式矢量图 + 工牌芯片（与既定口径一致）。
3. 验收结论（事实）：
   - `AC-UO-01 ~ AC-UO-12` 全部 `pass`（脚本结论 + 人工补验一致）。
   - 发布视图仅展示“已发布迭代（tag + release note）”；未在 API 视图暴露中间 `commit_ref`。
4. 风险与后续（推断/假设，置信度: 中）：
   - 当前 `commit_ref` 仍在 DB 内部存储用于追溯，后续 UI 改版时需回归验证“不外露 commit 明细”约束是否持续成立。

## 本轮更新（2026-03-03）- 训练中心发布治理范围扩展（需求收敛中）
1. 用户新增需求（明示）：
   - 希望“所有 agent 都在训练中心进行发布管理”。
   - 希望提供“版本切换”功能。
2. 当前状态（事实）：
   - 已进入需求收敛阶段，待确认版本切换边界与安全门禁后再进入设计。
3. 待确认重点（开放项）：
   - 切换目标对象（完整工作区/仅策略文件/仅发布元数据）。
   - 切换范围（仅已发布版本，还是允许回退到任意历史点）。
   - 触发门禁（是否需要二次确认、审批、自动回滚阈值）。

## 本轮更新（2026-03-03）- 版本切换约束补充确认（需求继续收敛）
1. 用户确认（明示）：
   - 所有 agent 均纳入训练中心发布管理。
   - 版本切换仅允许切到已发布版本，不允许切到中间 commit。
   - 暂不需要风险提示和操作留痕。
2. 用户新增运行约束（明示）：
   - agent 一旦执行版本切换，该 agent 不允许继续训练。
   - 允许克隆出新角色，并基于克隆角色的当前版本继续训练。
3. 当前状态（事实）：
   - 已接近设计门禁，仅剩“切换后冻结边界”和“克隆角色生命周期”需确认。

## 本轮更新（2026-03-03）- 发布切换后训练门禁与预发布治理（需求继续收敛）
1. 用户补充确认（明示）：
   - 原 agent 在“切换回最新发布版本”后，才允许恢复训练。
   - 克隆角色纳入统一发布管理。
   - 版本切换执行方式采用“直接覆盖工作区”（用户认为可恢复）。
2. 用户新增需求（明示）：
   - 发布版本之外的最新修改视为“预发布功能”。
   - 需要“舍弃预发布内容”能力。
3. 当前状态（事实）：
   - 发布治理主线已明确，剩余待收敛项主要是“预发布内容边界与舍弃动作范围”。

## 本轮更新（2026-03-03）- 预发布舍弃动作与发布评估流程补充
1. 用户确认（明示）：
   - “舍弃预发布内容”仅支持单个 agent 执行（风险动作，不做批量）。
   - 舍弃后状态回到“发布态”。
2. 用户新增流程定义（明示）：
   - 通常训练完成后，agent 进入“预发布态”。
   - 经过一段时间实际使用后，再进行发布评估与发布决策。
3. 当前状态（事实）：
   - 生命周期主流程已收敛为：
     1) 训练 -> 预发布；
     2) 实际使用观察；
     3) 发布评估；
     4) 通过则发布，不通过可继续训练或舍弃预发布改动。

## 本轮更新（2026-03-03）- 发布评估阶段策略与平台单元定义补充
1. 用户确认（明示）：
   - 当前发布评估先采用人工审核。
   - 后续引入评估机制后，目标之一是支持自动训练与自动发布。
2. 用户新增方向性定义（明示）：
   - agent 管理是后续功能的基石。
   - 对话功能与后续新增功能均以 agent 为基本单元构建。
3. 当前状态（事实）：
   - 需求门禁已基本收敛，可进入“需求概设 + 详细设计”输出阶段。

## 本轮更新（2026-03-03）- agent发布管理与版本切换文档交付
1. 用户确认（明示）：
   - 同意进入设计输出（“可以”）。
2. 已交付文档（事实）：
   - `docs/workflow/需求详情-agent发布管理与版本切换.md`
   - `docs/workflow/详细设计-agent发布管理与版本切换.md`
   - `docs/workflow/需求概述.md`（已新增索引与第 16 节增量需求）
3. 设计主线（事实）：
   - 全量 agent 纳管发布；
   - 仅已发布版本可切换；
   - 切换后原角色禁训，切回最新发布版本解冻；
   - 训练后进入预发布态，支持单 agent 舍弃预发布；
   - 当前人工评估，后续预留自动评估/自动发布扩展。

## 本轮更新（2026-03-03）- 跨工作区同步与分发提示词生成
1. 用户确认（明示）：
   - 同意执行“同步到 `../workflow` + 生成分发提示词”。
2. 已执行同步（事实）：
   - 已同步文件到 `../workflow/docs/workflow/`：
     1) `需求概述.md`
     2) `需求详情-agent发布管理与版本切换.md`
     3) `详细设计-agent发布管理与版本切换.md`
     4) `prompts/执行提示词-agent发布管理与版本切换-20260303.md`
3. 一致性校验（事实）：
   - 上述 4 个文件 SHA256 均一致（source == target）。
4. 提示词治理（事实）：
   - 新增提示词已落盘到 `docs/workflow/prompts/`。
   - 两侧提示词目录均执行“保留最近 7 轮”检查，当前未超限。

## 本轮更新（2026-03-03）- `../workflow` 提示词历史文件清理与验收证据口径澄清
1. 用户请求（明示）：
   - 删除 `../workflow` 多出的历史提示词文件。
   - 确认“仅门禁统计表截图”是否可作为功能验收证据。
2. 已执行动作（事实）：
   - 新增脚本：`scripts/sync-workflow-prompts.ps1`（按源目录同步并删除目标目录多余提示词）。
   - 已执行脚本删除多余文件：`执行提示词-会话详情顺序与评分机制优化-20260228.md`。
   - 清理后两侧提示词文件集一致（5 个）。
3. 证据口径结论（事实）：
   - 门禁统计表截图只能作为辅助证据，不能替代功能截图。
   - 每条 AC 仍需提交对应功能页面截图 + API + 日志/DB + 代码路径。

## 本轮更新（2026-03-03）- 证据不合格场景处理策略
1. 用户问题（明示）：
   - 当执行方仅提交门禁统计截图、缺少功能截图时如何处理。
2. 处理口径（事实）：
   - 判定为“证据不合格退回”，不是“功能实现失败”。
   - 对缺证 AC 标记 `fail_evidence`，要求按 AC 逐条补交功能证据。
3. 最小补交要求（事实）：
   - 功能截图（页面状态）+ API + 日志/DB + 代码路径；
   - 关键 AC 需录屏/GIF。

## 本轮更新（2026-03-03）- 证据语义门禁前置到交付文件
1. 用户问题（明示）：
   - 如何在交付文件里前置声明“门禁统计截图不可替代功能截图”。
2. 已执行动作（事实）：
   - 已在 2 份需求详情、2 份详细设计、2 份执行提示词、1 份 prompts 目录说明中固化“证据语义门禁”条款。
   - 关键条款：统计/汇总截图仅辅助，不计入 AC 功能截图。
3. 同步结果（事实）：
   - 已同步上述 7 个文件到 `../workflow/docs/workflow/` 并完成哈希一致性校验。

## 本轮更新（2026-03-03）- AR/UO 交付独立复验
1. 用户请求（明示）：
   - 对执行方回传的 AR/UO 结果进行验收，并给出下一步建议。
2. 已执行动作（事实）：
   - 独立复跑 AR：`python scripts/acceptance/run_acceptance_agent_release_ar.py --root C:/work/agents/workflow --host 127.0.0.1 --port 8128`。
   - 独立复跑 UO：`python scripts/acceptance/run_acceptance_training_center_uo.py --root C:/work/agents/workflow --host 127.0.0.1 --port 8129`。
3. 复验结果（事实）：
   - AR 脚本返回码 `0`，`AC-AR-01~10` 全部 `pass`。
   - UO 脚本返回码 `0`，`AC-UO-01~12` 全部 `pass`。
4. 人工补验（事实）：
   - 功能截图存在且非仅门禁统计图（`AC-AR-01~09` 与 `AC-UO-01~11` 均有功能页面截图）。
   - 风险提示：`AC-AR-09` 截图语义不够直观（截图中人工评估操作可见性偏弱），建议补一张“评估提交后状态”截图增强可审阅性。

## 本轮更新（2026-03-03）- 验收证据精简策略交付
1. 用户需求（明示）：
   - 每轮验证后不保留大量功能截图，仅保留门禁截图。
2. 已执行动作（事实）：
   - 在 `需求概述` 第 17 节新增“证据生命周期精简”策略。
   - 在 UO/AR 需求详情与两份详细设计中加入“验收后可精简，仅保留门禁截图+汇总文件”条款。
   - 新增执行提示词：
     - `docs/workflow/prompts/执行提示词-验收证据精简保留门禁截图-20260303.md`。
3. 同步与治理（事实）：
   - 上述文件已同步到 `../workflow/docs/workflow/` 并通过哈希一致性校验。
   - 两侧提示词目录已检查“仅保留最近 7 轮”，当前未超限。

## 本轮更新（2026-03-03）- 精简后复验能力边界澄清
1. 用户问题（明示）：
   - 发现功能截图已被清理后，如何继续验收。
2. 现状核查（事实）：
   - `agent-release-ar-20260303-140159` 与 `training-center-uo-20260303-140356` 当前均仅保留门禁截图与汇总文件。
3. 结论（事实）：
   - 仅保留门禁截图时，可做“门禁级复核”，不可做“功能级复核（逐 AC 界面语义核对）”。
4. 建议策略（事实）：
   - 采用双阶段：
     1) 验收阶段保留完整证据并完成复验；
     2) 验收通过后再执行精简脚本。

## 本轮更新（2026-03-03）- 测试数据展示开关与全局设置核查
1. 用户需求（明示）：
   - 希望 `../workflow` 将“测试数据展示开关”整合到全局设置。
2. 核查结果（事实）：
   - 当前页面结构中已存在“设置 > 全局配置 > 展示测试数据”开关。
   - 前端状态使用本地缓存键 `workflow.p0.settings.showTestData`，属于浏览器本地持久化。
3. 待收敛点（开放项）：
   - “全局设置”是否仅指 UI 位置统一，还是要求升级为后端全局配置（跨会话/跨浏览器一致）。

## 本轮更新（2026-03-03）- 会话入口测试数据控制不统一根因确认
1. 用户反馈（明示）：
   - 会话入口中的测试数据展示没有统一控制。
2. 代码核查结论（事实）：
   - 当前存在两套开关：
     1) 会话入口内联开关 `showSystemAgentsCheck`（控制 agent 列表“测试/系统 agent”可见性）；
     2) 设置页全局配置 `showTestDataCheck`（控制会话/队列/API 的 `include_test_data`）。
   - 两者语义与作用域不同，导致用户感知为“测试数据控制不统一”。
3. 收敛方向（事实）：
   - 需要统一为“单一全局开关 -> 多模块一致生效”的控制模型。

## 本轮更新（2026-03-03）- 测试数据展示开关全局统一需求交付
1. 用户确认（明示）：
   - 统一控制模型：`1. 统一`。
   - 全局语义要求：`2. 要的`（需后端持久化，不是仅前端本地缓存）。
2. 已交付文档（事实）：
   - `docs/workflow/需求详情-测试数据展示全局开关统一.md`
   - `docs/workflow/详细设计-测试数据展示全局开关统一.md`
   - `docs/workflow/需求概述.md`（新增第 18 节与索引）
   - `docs/workflow/prompts/执行提示词-测试数据展示开关全局统一-20260303.md`
3. 同步与治理（事实）：
   - 上述文件已同步至 `../workflow/docs/workflow/`。
   - 四文件 SHA256 一致性校验通过。
   - 两侧 `prompts/` 目录已执行“仅保留最近 7 轮”检查，当前均为 7 轮。
4. 当前收敛口径（事实）：
   - 删除会话入口独立测试/系统开关。
   - 保留全局设置唯一开关。
   - 全局开关由后端持久化并统一驱动会话入口、Dashboard、会话列表、训练中心。


## 本轮更新（2026-03-03）- `../workflow` 代码体积治理与无损重构交付
1. 用户需求（明示）：
   - 让 `../workflow` 启动重构，当前文件过大。
   - 前提：不能影响目前工作效果。
2. 已执行动作（事实）：
   - 新增需求文档：`docs/workflow/需求详情-代码体积治理与无损重构.md`。
   - 新增详细设计：`docs/workflow/详细设计-代码体积治理与无损重构.md`。
   - 更新概述索引：`docs/workflow/需求概述.md` 新增第 19 节。
   - 新增执行提示词：`docs/workflow/prompts/执行提示词-代码体积治理与无损重构-20260303.md`。
3. 同步与校验（事实）：
   - 上述文件已同步至 `../workflow/docs/workflow/`。
   - 四文件 SHA256 一致性校验通过。
   - 两侧 prompts 目录已执行“仅保留最近 7 轮”清理并对齐。

## 本轮更新（2026-03-03）- 本工作区状态档案无损归档瘦身
1. 用户需求（明示）：
   - 本工作区快照与偏好文件体量较大，需要治理，且不影响当前工作效果。
2. 已执行动作（事实）：
   - 新增脚本：`scripts/archive-analyst-state.ps1`（先备份再归档）。
   - 新增归档目录与索引：
     1) `workspace_state/logs/session-history/` + `workspace_state/logs/session-history-index.md`
     2) `user_profile/logs/change-log-history/` + `user_profile/logs/change-log-history-index.md`
   - 首次归档完成：
     1) `session-snapshot.md`：1492 行 -> 241 行
     2) `thinking-patterns-change-log.md`：1089 行 -> 193 行
   - 备份路径：`.tmp/state-backups/20260303-163656/`
3. 机制联动更新（事实）：
   - `workspace_state/core/startup-checklist.md` 新增归档索引检查与归档触发自检。
   - `workspace_state/core/workspace-state-overview.md`、`workspace_state/logs/state-change-log.md` 已同步更新。
4. 风险与结论（推断/假设，置信度: 中）：
   - 当前为结构无损迁移（历史内容已归档可追溯），后续若继续增长可按同脚本周期执行。

## 本轮更新（2026-03-03）- 长期偏好 200 行门禁与评审机制落地
1. 用户需求（明示）：
   - 长期偏好应保持精简，单文件最好不超过 200 行。
   - 超过 200 行时，必须启动评审：保留长期项，其余降级为短期偏好。
2. 已执行动作（事实）：
   - 更新规则入口：`AGENTS.md` 新增长期偏好行数治理与超限评审规则。
   - 新增短期池：`user_profile/core/thinking-patterns-short-term.md`。
   - 新增评审日志：`user_profile/governance/preference-review-log.md`。
   - 新增自动门禁脚本：`scripts/check-long-term-preference-cap.ps1`。
   - 更新启动检查：`workspace_state/core/startup-checklist.md` 新增偏好行数门禁检查。
3. 首次超限评审结果（事实）：
   - 触发文件：`user_profile/core/thinking-patterns-domain-requirements.md`（原 276 行）。
   - 处理结果：保留长期主干，四个增量专题块降级到短期偏好池。
   - 评审后行数：`thinking-patterns-domain-requirements.md` -> 156 行。
4. 自动校验结果（事实）：
   - 执行 `scripts/check-long-term-preference-cap.ps1 -WriteQueue` 通过。
   - 当前长期主档行数：global=42、requirements=156、collaboration=161、overview=149（均 <= 200）。
5. 风险与控制（推断/假设，置信度: 中）：
   - 若后续短期池持续增长，需按同样机制定期评审，避免短期池再次失控。

## 本轮补充（2026-03-03）- 长期偏好门禁生效校验
1. 已执行 `scripts/check-long-term-preference-cap.ps1` 并通过。
2. 当前长期主档行数：global=42、requirements=156、collaboration=161、overview=150。
3. 口径补充：`thinking-patterns-change-log.md` 作为流水日志，不纳入 200 行上限。

## 本轮更新（2026-03-03）- 技能维护增强与示例门禁
1. 用户需求（明示）：
   - 评估是否需要新增技能以方便维护。
   - 所有技能必须包含示例。
2. 已执行动作（事实）：
   - 新增技能：`.codex/skills/preference-cap-review-governor/SKILL.md`（长期偏好 200 行上限评审专用）。
   - 为以下技能补充 `## Examples`：
     1) `method-selection-orchestrator`
     2) `proactive-practice-advisor`
     3) `session-state-maintainer`
     4) `snapshot-archive-governor`
   - 新增检查脚本：`scripts/check-skill-examples.ps1`（校验每个本地技能都含 `## Examples`）。
3. 校验结果（事实）：
   - 执行 `scripts/check-skill-examples.ps1` 通过。
4. 机制联动（事实）：
   - `skills/local-skills-overview.md` 已加入新技能与“技能示例门禁”条款。
   - `AGENTS.md` 已加入“本地技能必须包含示例”规则。

## 本轮更新（2026-03-03）- 工程化重构与目录治理专项交付
1. 用户背景（明示）：
   - 执行方反馈当前仍是“核心服务单文件聚合”，仅完成了部分规范化。
   - 用户要求推进“工程化重构 + 目录结构工程化”，且保持无损。
2. 已执行动作（事实）：
   - 新增需求：`docs/workflow/需求详情-工程化重构与目录治理.md`。
   - 新增设计：`docs/workflow/详细设计-工程化重构与目录治理.md`。
   - 更新概述：`docs/workflow/需求概述.md` 新增第 20 节与索引。
   - 新增提示词：`docs/workflow/prompts/执行提示词-工程化重构与目录治理-20260303.md`。
3. 约束强化点（事实）：
   - 明确禁止“空壳分包”，要求实质迁移 `do_GET/do_POST`、模板、DB/迁移、Agent 解析链路。
   - 增加分阶段门禁（Phase0~4）与阶段回滚要求。
4. 同步与治理（事实）：
   - 上述 4 个文件已同步到 `../workflow/docs/workflow/`。
   - 4 文件 SHA256 一致性校验通过。
   - 两侧 prompts 均已执行“保留最近 7 轮”清理。

## 本轮更新（2026-03-03）- workflow命名与去原型化治理方案本地预备
1. 用户需求（明示）：
   - 当前运行时名称“训练闭环工作台”不合适。
   - `Phase0` 与 `01_*`/`02_*` 等原型命名不适合长期产品。
2. 已执行动作（事实）：
   - 新增需求文档：`docs/workflow/需求详情-workflow命名与去原型化治理.md`。
   - 新增设计文档：`docs/workflow/详细设计-workflow命名与去原型化治理.md`。
   - 更新概述：`docs/workflow/需求概述.md` 新增第 21 节与索引。
3. 当前口径（事实）：
   - 对外主名称统一为 `workflow`（展示可选 `Workflow`）。
   - 阶段词仅保留在里程碑文档，不进入运行时与核心业务命名。
   - 文件命名从序号式迁移为“领域 + 职责”语义。
4. 交付状态（事实）：
   - 本轮为“本地预备”，尚未同步到 `../workflow`，待用户下一步指令再交付并生成提示词。

## 本轮更新（2026-03-03）- 工程化重构硬门禁加强（继续压降大文件）
1. 用户反馈（明示）：
   - 执行方重构后主文件仍有 8000+ 行，要求继续推进实质拆分。
2. 已执行动作（事实）：
   - 已补强 `docs/workflow/需求详情-工程化重构与目录治理.md`：
     1) 新增硬门禁 FR（`workflow_web_server.py <= 3000`、`server/api/legacy.py <= 1000`）；
     2) 新增“任一硬门禁失败即 `fail_hard_gate`”完成判定；
     3) 新增命名去原型化约束（禁 `Phase*`/`01_*`/`02_*`）。
   - 已补强 `docs/workflow/详细设计-工程化重构与目录治理.md`：
     1) 分阶段命名改为“阶段 A~E”；
     2) 新增“硬门禁判定规则”章节与交付判定口径。
   - 已重写并补强 `docs/workflow/prompts/执行提示词-工程化重构与目录治理-20260303.md`：
     1) 明确硬门禁与失败标记；
     2) 强制回传 Top15 行数前后对比；
     3) 固化“功能截图不可被门禁统计截图替代”。
3. 同步与校验（事实）：
   - 已同步上述 3 个文件到 `../workflow/docs/workflow/`。
   - 3 文件 SHA256 一致性校验通过。
   - 两侧提示词目录仍保持最近 7 轮。
4. 当前待办（下一轮优先）：
   - 等待用户下发新提示词给执行方后，接收其回传并按硬门禁复验（优先核对 2 个行数阈值与 Top15 报告真实性）。

## 本轮更新（2026-03-03）- `../workflow` 工程化重构回传复核
1. 用户输入（明示）：
   - 用户提供执行方回传摘要，要求评估“这轮重构是否到位”。
2. 独立核验结果（事实）：
   - 关键硬门禁命中：
     1) `src/workflow_app/workflow_web_server.py` 为 11 行；
     2) `src/workflow_app/server/api/legacy.py` 为 6 行。
   - 关键迁移事实：
     1) 运行主体迁移至 `src/workflow_app/server/bootstrap/web_server_runtime.py`（8243 行）；
     2) legacy 主体迁移至 `src/workflow_app/server/api/legacy_route_handlers.py`（1929 行）。
   - 路由域拆分存在：
     1) `server/api/router.py` 已分发到 `config/chat/training/policy/dashboard` + `legacy`。
3. 评估结论（事实 + 推断/假设，置信度: 高）：
   - “硬门禁通过”成立；
   - “工程化重构完成”不成立（仍是单大文件搬迁，未达到长期可维护目标）。
4. 主要残余风险（事实）：
   - `web_server_runtime.py` 仍含 `Handler/do_GET/do_POST` 主干；
   - `legacy_route_handlers.py` 仍为超阈值聚合文件；
   - 运行时文案仍出现 `Phase0` 表述，和去原型化方向不一致。

## 本轮更新（2026-03-03）- 工程化重构二轮提示词（反规避硬门禁）交付
1. 用户确认（明示）：
   - 同意生成并下发下一轮“反规避硬门禁”执行提示词。
2. 已执行动作（事实）：
   - 新增提示词文件：
     - `docs/workflow/prompts/执行提示词-工程化重构反规避硬门禁-20260303.md`
   - 核心新增约束：
     1) `web_server_runtime.py <= 3000`；
     2) `legacy_route_handlers.py <= 1000`；
     3) 禁止“新单体替代旧单体”；
     4) 运行时代码去 `Phase0/Phase1` 文案。
3. 同步与治理（事实）：
   - 已同步至 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录均保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-角色提取主链路切换CodexAgent-20260301.md`。

## 本轮更新（2026-03-04）- 工程化重构第三轮体积收敛提示词交付
1. 用户请求（明示）：
   - 基于执行方“二轮已通过”结果，继续推进后续优化。
2. 独立抽检（事实）：
   - 当前关键体量：
     1) `server/bootstrap/web_server_runtime.py` 2248；
     2) `training_center_runtime.py` 2139；
     3) `server/services/policy_analysis.py` 1688；
     4) `server/api/legacy_task_handlers.py` 1478；
     5) `server/api/legacy_route_handlers.py` 15。
3. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构三轮体积收敛-20260304.md`
   - 三轮硬门禁核心：
     1) `training_center_runtime.py <= 1500`；
     2) `policy_analysis.py <= 1200`；
     3) `legacy_task_handlers.py <= 900`；
     4) 回归守门：`web_server_runtime.py <= 3000`、`legacy_route_handlers.py <= 1000`。
4. 同步与治理（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录均保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-统一入口与训练优化模块-20260302.md`。

## 本轮更新（2026-03-04）- “千万级可维护”结构治理方案讨论启动
1. 用户观点（明示）：
   - 若模块化与 README 契约充分，agent 理论维护规模可接近无上限（含千万级代码量）。
2. 当前任务（事实）：
   - 输出 `../workflow` 的结构治理方案，使其更适合 agent 长期维护。
3. 输出方向（推断/假设，置信度: 高）：
   - 需同时覆盖：
     1) 代码结构分层与领域边界；
     2) 模块 README 标准模板；
     3) 自动化门禁与可观测度；
     4) 分阶段落地路径与度量指标。

## 本轮更新（2026-03-04）- 多仓兼容与新仓重建约束确认
1. 用户补充（明示）：
   - 接受“未来可拆多仓”方向；
   - 不一定在历史仓直接改动，可能新建仓库以更合理方式重建同等功能，避免影响历史项目。
2. 需求影响（事实）：
   - 方案需从“单仓重构”升级为“旧仓保护 + 新仓重建 + 可迁移切换”双轨治理。
3. 下一步输出方向（推断/假设，置信度: 高）：
   - 交付内容需包含：
     1) 多仓边界与职责划分；
     2) 兼容层/契约层设计；
     3) 影子验证与切换策略；
     4) 旧仓冻结与回滚机制。

## 本轮更新（2026-03-04）- 路线收敛为“现仓优先重构”
1. 用户最新决策（明示）：
   - 短期暂不新建仓，先在 `../workflow` 现仓持续重构，以提升可维护性。
2. 策略调整（事实）：
   - 当前阶段按“单仓工程化治理”推进；
   - 多仓/新仓重建仅保留为中长期备选，不进入当前执行范围。
3. 下一步执行焦点（事实）：
   - 继续按已下发“三轮体积收敛”门禁，优先拆分剩余超阈值大文件并保持功能等价。

## 本轮更新（2026-03-04）- `../workflow` 结构审查 + 第四轮提示词交付
1. 用户请求（明示）：
   - 在继续优化同时，检查 `../workflow` 当前目录结构是否符合预期工程化形态。
2. 结构审查结果（事实）：
   - 已形成后端分层目录：`server/api|services|infra|presentation|bootstrap`。
   - API/服务层拆分已有明显进展（如 `legacy_task_handlers` 已进一步拆域，`policy_analysis` 与 `training_center_runtime` 已显著降行）。
   - 主要缺口：
     1) 模块 README 契约化不足（`server` 及其子目录缺 README）；
     2) 运行时代码仍有 `Phase0` 文案残留（`workflow_entry_cli.py`、`web_client/03_*`）；
     3) 前端仍以序号大文件为主，域化边界说明不足。
3. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构四轮结构契约化-20260304.md`
   - 目标聚焦：
     1) 补齐模块 README 契约；
     2) 清理运行时代码 Phase 文案；
     3) 增加结构门禁脚本（README 契约检查 + 文案扫描）。
4. 同步与治理（事实）：
   - 提示词已同步至 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-agent发布管理与版本切换-20260303.md`。

## 本轮更新（2026-03-04）- 结构不满意复核与第五轮大文件专项交付
1. 用户反馈（明示）：
   - 对当前工程结构仍不满意，认为仍有较多大文件影响维护性。
2. 独立复核（事实）：
   - 当前源码仍有 8 个文件 > 1200 行：
     1) `server/presentation/templates/index.html` 2513
     2) `server/bootstrap/web_server_runtime.py` 2248
     3) `web_client/03_policy_confirm_and_interactions.js` 1955
     4) `web_client/02_session_and_agent_meta.js` 1610
     5) `web_client/04_workflow_queue_and_batch.js` 1484
     6) `web_client/00_app_state_and_utils.js` 1471
     7) `server/services/training_workflow.py` 1445
     8) `workflow_entry_cli.py` 1351
3. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构五轮大文件专项-20260304.md`
   - 新增硬门禁覆盖上述 8 个文件，要求本轮收敛到阈值内，并加入“新增源码文件不得 >1500”防回流规则。
4. 同步与治理（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-验收证据精简保留门禁截图-20260303.md`。

## 本轮更新（2026-03-04）- 第六轮命名规范化提示词交付
1. 用户反馈（明示）：
   - 对 `00/01/02` 序号文件命名表示不合理，要求纳入重构治理。
2. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构六轮命名规范化-20260304.md`
   - 核心要求：
     1) 去除 `web_client` 序号前缀命名；
     2) 从“文件名排序”迁移到“显式装配清单”；
     3) 保持 `/static/workflow-web-client.js` 对外路径兼容。
3. 同步与治理（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-测试数据展示开关全局统一-20260303.md`。

## 本轮更新（2026-03-04）- 五六轮提示词合并交付
1. 用户请求（明示）：
   - 将“五轮大文件专项”和“六轮命名规范化”提示词合并；当前尚未发起五轮执行。
2. 已执行动作（事实）：
   - 新增合并提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构五六轮合并-20260304.md`
   - 合并范围：
     1) 大文件收敛硬门禁；
     2) 去序号命名 + 显式 manifest 装配门禁；
     3) UO/AR 与结构脚本回归门禁。
   - 为避免执行歧义，已下线旧文件：
     1) `执行提示词-工程化重构五轮大文件专项-20260304.md`
     2) `执行提示词-工程化重构六轮命名规范化-20260304.md`
3. 同步与校验（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧当前执行提示词文件集一致（6 个）。

## 本轮更新（2026-03-04）- `../workflow` 重构后结构与体量独立复核
1. 用户请求（明示）：
   - 对执行方“已完成重构”进行独立复核，重点看工程目录是否合理、文件体量是否健康。
2. 结构复核（事实）：
   - 后端分层目录保持存在：`server/api|services|infra|presentation|bootstrap`。
   - 前端序号命名已移除，`web_client` 已改为语义文件名，并提供 `bundle_manifest.json` 显式装配清单。
   - 模块契约 README 存在：`server/` 及其 5 个子目录均有 `README.md`。
3. 体量复核（事实，源码口径）：
   - Top1 大文件：`server/bootstrap/web_server_runtime.py` 1237 行。
   - 源码文件统计：`>1200` 仅 1 个，`>1500` 为 0，`>2000` 为 0。
   - `>1000` 文件共 10 个，主要集中于 `web_client/` 与 `server/services/`。
4. 发现风险（事实）：
   - 文档一致性风险：`server/bootstrap/README.md` 仍写“按文件名排序拼装”，与当前 manifest 装配实现不一致。
   - 可读性风险：`server/presentation/templates/index.css` 为超长单行（约 39KB），虽行数低但可维护性较差。

## 本轮更新（2026-03-04）- `workflow_app/__init__.py` 存在原因澄清
1. 用户问题（明示）：
   - 询问 `C:/work/agents/workflow/src/workflow_app/__init__.py` 为何存在。
2. 核查结果（事实）：
   - 文件内容仅为包级说明字符串。
   - 当前 `scripts/*.py` 通过 `sys.path` 注入 `src` 后，使用 `from workflow_app...` 导入主模块；`workflow_app` 作为显式包存在。
3. 结论（事实 + 推断/假设，置信度: 高）：
   - `__init__.py` 主要用于明确包边界与提升工具链兼容性，不是冗余运行文件。

## 本轮更新（2026-03-04）- 第七轮“边界收敛 + 根目录瘦身”提示词交付
1. 用户确认（明示）：
   - 同意下发“只改边界不动功能”的收敛提示词。
   - 新增目标：`src/workflow_app` 根目录进一步简洁，避免过多源码文件堆在根层。
2. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构七轮边界收敛与根目录瘦身-20260304.md`
   - 核心门禁：
     1) `api/services` 禁止依赖 `workflow_app.workflow_web_server`；
     2) `bootstrap` 禁止直接导入历史根层大模块；
     3) 根目录 `.py` 文件数与“仅门面转发”约束；
     4) README 与实现一致性门禁。
3. 同步与校验（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。

## 本轮更新（2026-03-04）- 第八轮 scripts 目录治理提示词交付
1. 用户确认（明示）：
   - 同意新增 “scripts 顶层瘦身” 重构轮次。
2. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工程化重构八轮scripts目录治理-20260304.md`
   - 核心门禁：
     1) `scripts/` 顶层文件数 `<= 4`；
     2) `check-*` 归类到 `scripts/quality/`；
     3) 启动脚本归类到 `scripts/dev/`；
     4) 旧命令兼容抽测与 UO/AR 回归通过。
3. 同步与治理（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-代码体积治理与无损重构-20260303.md`。

## 本轮更新（2026-03-04）- 工牌与版本发布信息中文化提示词交付
1. 用户需求（明示）：
   - 希望 `../workflow` 的工牌与版本发布信息使用中文展示。
2. 已执行动作（事实）：
   - 新增提示词：
     - `docs/workflow/prompts/执行提示词-工牌与版本发布信息中文化-20260304.md`
   - 约束范围：
     1) 仅改显示层文案，不改 API 字段与业务流程；
     2) 中文术语全局一致（工牌 + 发布管理）；
     3) UO + AR 回归通过。
3. 同步与治理（事实）：
   - 已同步到 `../workflow/docs/workflow/prompts/` 并完成 SHA256 一致性校验。
   - 两侧提示词目录保持最近 7 轮。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-工程化重构与目录治理-20260303.md`。

## 本轮更新（2026-03-04）- 全站中文化需求与提示词交付
1. 用户输入（明示）：
   - 引用了执行方“下一步建议”，并明确希望将“全站中文化（允许少量常用短英文保留）”写入需求。
2. 已执行动作（事实）：
   - 新增需求文档：
     1) `docs/workflow/需求详情-全站中文化与术语白名单.md`
     2) `docs/workflow/详细设计-全站中文化与术语白名单.md`
   - 更新索引：
     1) `docs/workflow/需求概述.md` 新增第 22 节。
   - 新增提示词：
     1) `docs/workflow/prompts/执行提示词-全站中文化与术语白名单-20260304.md`
3. 同步与校验（事实）：
   - 已同步上述 4 个文件至 `../workflow/docs/workflow/`，并完成 SHA256 一致性校验。
   - 提示词目录执行“保留最近 7 轮”后，两侧文件集一致。
   - 本轮按保留窗口删除旧文件：
     - `执行提示词-工程化重构反规避硬门禁-20260303.md`。

## 本轮更新（2026-03-04）- 全站中文化白名单扩展（工程术语）
1. 用户补充（明示）：
   - 白名单可加入工程常用术语（示例：`project`）。
2. 已执行动作（事实）：
   - 已在以下 3 个文件扩展白名单词表：
     1) `需求详情-全站中文化与术语白名单.md`
     2) `详细设计-全站中文化与术语白名单.md`
     3) `prompts/执行提示词-全站中文化与术语白名单-20260304.md`
   - 新增术语包含：
     - `project`、`repo`、`branch`、`commit`、`tag`、`PR`、`CI/CD`。
3. 同步与校验（事实）：
   - 已同步上述 3 文件到 `../workflow/docs/workflow/`。
   - 3 文件 SHA256 一致性校验通过。

## 本轮更新（2026-03-04）- 全站中文化白名单补充 `AGENT`
1. 用户补充（明示）：
   - 白名单中可加入 `AGENT`。
2. 已执行动作（事实）：
   - 已更新以下 3 文件白名单词表，加入 `AGENT`：
     1) `需求详情-全站中文化与术语白名单.md`
     2) `详细设计-全站中文化与术语白名单.md`
     3) `prompts/执行提示词-全站中文化与术语白名单-20260304.md`
3. 同步与校验（事实）：
   - 已同步到 `../workflow/docs/workflow/`。
   - 3 文件 SHA256 一致性校验通过。

## 本轮更新（2026-03-04）- `scripts/` 顶层文件密度复核
1. 用户问题（明示）：
   - 询问 `C:/work/agents/workflow/scripts` 顶层文件为何较多。
2. 现状核查（事实）：
   - 顶层文件 13 个，主要分三类：
     1) 兼容薄入口（`workflow_web_server.py`、`workflow_entry_cli.py` 等）；
     2) 质量/门禁脚本（`check-*`）；
     3) 启动脚本（`launch_workflow.ps1`、`start_workflow_web.ps1`）。
3. 结论（事实 + 推断/假设，置信度: 高）：
   - 当前并非“无序堆积”，但顶层信息密度偏高，仍有进一步目录分层空间（`bin/`, `quality/`, `dev/`）。

## 本轮更新（2026-03-05）- `../workflow` 需求命名替换
1. 用户请求（明示）：
   - 在 `../workflow` 的需求内容中，将“训练闭环工作台”替换为 `workflow`。
2. 已执行动作（事实）：
   - 已更新 `../workflow/docs/workflow/需求概述.md` 的对应文本（1 处命中）。
   - 本轮仅修改需求文档，未改动代码与验收证据文件。
3. 校验结果（事实）：
   - 已确认 `../workflow/docs/workflow/需求概述.md` 不再包含“训练闭环工作台”文本。

## 本轮更新（2026-03-05）- 下发“workflow运行时命名替换”执行提示词
1. 用户确认（明示）：
   - 同意下发执行方任务（“下发吧”）。
2. 已执行动作（事实）：
   - 新增提示词：`docs/workflow/prompts/执行提示词-workflow运行时命名替换-20260305.md`。
   - 按“仅保留最近 7 轮”清理旧提示词：删除 `执行提示词-工程化重构三轮体积收敛-20260304.md`。
   - 已同步到 `../workflow/docs/workflow/prompts/`（7/7 一致）。
3. 一致性校验（事实）：
   - 新提示词双侧 SHA256 一致：
     - `3066283AC2F8A4AE6E2863185ACD4E3E57EDD9DB4C26B50D515466C7682E739D`。
