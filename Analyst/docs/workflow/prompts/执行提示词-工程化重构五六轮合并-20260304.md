# 执行提示词-工程化重构五六轮合并-20260304

你现在在 `../workflow` 工作区执行“工程化重构五六轮合并任务”。  
本轮一次完成两件事：
1. 大文件专项收敛（原第五轮目标）；
2. 前端命名规范化与显式装配（原第六轮目标）。

禁止新增功能，只做结构优化与等价重构。

## 1. 先读文件（必做）
1. `docs/workflow/requirements/需求详情-工程化重构与目录治理.md`
2. `docs/workflow/design/详细设计-工程化重构与目录治理.md`
3. `docs/workflow/reports/ARCHITECTURE_REFACTOR_MAP.md`
4. `docs/workflow/reports/REFactor_LINE_BUDGET_REPORT.md`
5. `src/workflow_app/web_client/README.md`

## 2. 执行阶段（必须按顺序）
1. 阶段 A：大文件收敛
   1. `src/workflow_app/server/bootstrap/web_server_runtime.py`
   2. `src/workflow_app/server/services/training_workflow.py`
   3. `src/workflow_app/workflow_entry_cli.py`
   4. `src/workflow_app/server/presentation/templates/index.html`
   5. `src/workflow_app/web_client/03_policy_confirm_and_interactions.js`
   6. `src/workflow_app/web_client/02_session_and_agent_meta.js`
   7. `src/workflow_app/web_client/04_workflow_queue_and_batch.js`
   8. `src/workflow_app/web_client/00_app_state_and_utils.js`
2. 阶段 B：命名规范化
   1. 去除 `web_client` 下所有 `^\d{2}_.*\.js$` 命名；
   2. 前端组装从“按文件名排序”改为“显式 manifest 清单”；
   3. 保持 `/static/workflow-web-client.js` 对外路径不变。
3. 阶段 C：回归与证据
   1. 执行结构门禁脚本；
   2. 执行 UO + AR 验收；
   3. 输出本轮证据包。

## 3. 合并硬门禁（任一失败即 `fail_hard_gate`）
1. 大文件门禁：
   1. `web_server_runtime.py <= 1500`
   2. `training_workflow.py <= 900`
   3. `workflow_entry_cli.py <= 900`
   4. `index.html <= 1200`
   5. `03_policy_confirm_and_interactions.js <= 1200`
   6. `02_session_and_agent_meta.js <= 1200`
   7. `04_workflow_queue_and_batch.js <= 1200`
   8. `00_app_state_and_utils.js <= 1200`
2. 防回流门禁：
   1. `src/workflow_app/server/api/legacy_route_handlers.py <= 1000`
   2. `src/workflow_app/server/api/legacy_task_handlers.py <= 1000`
   3. 任一新增源码文件（`.py/.js/.html/.css`）不得 `> 1500` 行。
3. 命名与装配门禁：
   1. `src/workflow_app/web_client/` 下不得存在 `^\d{2}_.*\.js$` 文件；
   2. 前端组装逻辑不得使用“按文件名排序”；
   3. 必须有显式装配清单（manifest）。
4. 结构门禁回归：
   1. `scripts/check-module-readme-contract.py` 通过；
   2. `scripts/check-prototype-text.py` 通过。
5. 验收门禁：
   1. UO + AR 脚本均 `exit code 0`；
   2. `/static/workflow-web-client.js` 可访问且页面可用。

## 4. 禁止事项
1. 禁止夹带新功能需求。
2. 禁止只做“重命名”不做装配机制迁移。
3. 禁止只做“代码搬家”不做职责下沉。
4. 禁止只回传门禁统计截图，缺少功能截图/API/日志/代码路径。

## 5. 必交证据
1. 体量与门禁：
   1. `hard_gate_v56_counts.txt`
   2. `line_budget_v56.json`
   3. `top20_before.txt`
   4. `top20_after.txt`
2. 命名与装配：
   1. `naming_v56_report.json`（旧名 -> 新名映射）
   2. `assembly_v56_report.md`（旧排序装配 -> 新 manifest 装配）
   3. `ui_bundle_healthz.json`
3. 文档：
   1. 更新 `docs/workflow/reports/ARCHITECTURE_REFACTOR_MAP.md`
   2. 更新 `docs/workflow/reports/REFactor_LINE_BUDGET_REPORT.md`
4. AC 证据：
   1. 功能截图；
   2. API/脚本输出；
   3. 日志/DB 路径；
   4. 代码路径（文件:行号）。

## 6. 行数统计口径（固定）
1. 仅统计源码文件：`.py/.js/.html/.css`。
2. 排除目录：`__pycache__/`、`.output/`、`.test/`、`state/`。
3. 必须回传可复核的原始统计输出。

## 7. 回传格式（固定）
1. 变更摘要（文件列表 + 目的）。
2. AC 清单（`AC-ID | pass/fail | evidence`）。
3. 合并硬门禁结果（`gate-id | pass/fail | evidence`）。
4. 风险与未完成项（若有）。
5. 下一步建议（最多 3 条）。


