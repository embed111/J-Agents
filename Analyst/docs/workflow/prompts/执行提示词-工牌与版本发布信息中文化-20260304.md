# 执行提示词-工牌与版本发布信息中文化-20260304

你现在在 `../workflow` 工作区执行“工牌与版本发布信息中文化”改造。  
本轮只做文案与展示语义统一，不做功能流程变更。

## 1. 先读文件（必做）
1. `docs/workflow/overview/需求概述.md`
2. `docs/workflow/requirements/需求详情-agent发布管理与版本切换.md`
3. `docs/workflow/design/详细设计-agent发布管理与版本切换.md`

## 2. 本轮必须完成
1. 工牌（agent 角色卡）中文化：
   1. 卡片主标题、副标题、状态标签、动作按钮统一中文。
   2. 版本字段、核心功能字段、生命周期字段统一中文术语。
2. 版本发布信息中文化：
   1. 发布列表、发布详情、版本状态、发布时间、发布说明全部中文展示。
   2. “预发布/已发布/已切换/当前版本/最新发布版本”等状态词统一中文。
3. 语义一致性：
   1. 同一概念全局使用同一中文词，不允许页面 A/B 混用中英文。
   2. 保留内部字段与 API 英文命名，不改协议字段，仅改显示层文案。
4. 兼容性：
   1. 不改动 API 路径与响应字段。
   2. UO + AR 验收脚本通过。

## 3. 硬门禁（任一失败即 `fail_hard_gate`）
1. 文案门禁：
   1. 工牌与发布管理主路径页面不应出现残留英文 UI 文案（品牌名 `workflow` 可保留）。
2. 一致性门禁：
   1. 同一术语在多个页面中文表达一致（例如“预发布”不能混成 `pre-release`）。
3. 回归门禁：
   1. UO + AR 脚本均 `exit code 0`。

## 4. 禁止事项
1. 禁止修改后端协议字段名。
2. 禁止改变发布/切换/训练门禁业务逻辑。
3. 禁止夹带无关重构。

## 5. 必交证据
1. `ui_zh_copy_map.md`：
   1. 英文旧文案 -> 中文新文案映射表。
2. `ui_zh_scan_report.txt`：
   1. 工牌与发布视图残留英文扫描结果。
3. 功能截图（必须）：
   1. 工牌概览页中文截图；
   2. agent 详情与发布历史中文截图；
   3. 版本切换/预发布状态中文截图。
4. 回归证据：
   1. UO/AR 脚本输出摘要 + 退出码。

## 6. 回传格式（固定）
1. 变更摘要（文件列表 + 目的）。
2. AC 清单（`AC-ID | pass/fail | evidence`）。
3. 中文化硬门禁结果（`gate-id | pass/fail | evidence`）。
4. 风险与未完成项（若有）。
5. 下一步建议（最多 3 条）。


