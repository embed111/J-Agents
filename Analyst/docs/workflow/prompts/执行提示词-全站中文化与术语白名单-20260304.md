# 执行提示词-全站中文化与术语白名单-20260304

你现在在 `../workflow` 工作区执行“全站中文化与术语白名单”改造。  
本轮只做展示层中文化与术语治理，不做业务逻辑改造。

## 1. 先读文件（必做）
1. `docs/workflow/overview/需求概述.md`（重点第 22 节）
2. `docs/workflow/requirements/需求详情-全站中文化与术语白名单.md`
3. `docs/workflow/design/详细设计-全站中文化与术语白名单.md`

## 2. 本轮必须完成
1. 全站中文化范围：
   1. 工牌概览与详情；
   2. 发布管理与版本切换；
   3. 训练运营关键界面；
   4. 会话入口与设置中的用户可见文案。
2. 建立词典与白名单：
   1. 输出 `ui_zh_copy_map.md`（旧文案 -> 新文案 -> 页面路径）。
   2. 输出 `ui_zh_whitelist.txt`（允许英文保留术语）。
3. 建立扫描报告：
   1. 输出 `ui_zh_scan_report.txt`（白名单命中 + 非白名单残留）。
4. 协议兼容：
   1. 不改 API 字段、路径、错误码语义。
   2. UO + AR 回归通过。

## 3. 中文化硬门禁（任一失败即 `fail_hard_gate`）
1. Gate-ZH-01：
   1. 工牌与发布管理页面主文案中文化完成。
2. Gate-ZH-02：
   1. 白名单外英文残留数必须为 0。
3. Gate-ZH-03：
   1. 同一术语全站中文表达一致，不得中英混写漂移。
4. Gate-ZH-04：
   1. UO + AR 脚本均 `exit code 0`。

## 4. 建议白名单（可按文档调整）
1. `workflow`
2. `AGENT`
3. `project`
4. `repo`
5. `branch`
6. `commit`
7. `tag`
8. `PR`
9. `CI/CD`
10. `API`
11. `ID`
12. `URL`
13. `UI`
14. `CLI`
15. `JSON`
16. `SQL`
17. `Git`
18. `CPU`

## 5. 禁止事项
1. 禁止改动后端协议字段名。
2. 禁止改动发布/切换/训练业务规则。
3. 禁止夹带无关重构。
4. 禁止只回传门禁统计截图，缺少功能截图。

## 6. 必交证据
1. `ui_zh_copy_map.md`
2. `ui_zh_whitelist.txt`
3. `ui_zh_scan_report.txt`
4. 功能截图：
   1. 工牌概览中文化截图；
   2. 发布历史中文化截图；
   3. 版本切换与预发布状态中文化截图；
   4. 训练运营中文化截图。
5. 回归证据：
   1. UO/AR 输出摘要 + 退出码。

## 7. 回传格式（固定）
1. 变更摘要（文件列表 + 目的）。
2. AC 清单（`AC-ID | pass/fail | evidence`）。
3. 中文化硬门禁结果（`gate-id | pass/fail | evidence`）。
4. 风险与未完成项（若有）。
5. 下一步建议（最多 3 条）。


