# 2026-03-14

## Confirmed

- Added an openclaw-style continuity scaffold under `.codex/`.
- GitHub submodule pull became workable in this workspace when Git used a process-local proxy plus:
  - `http.sslBackend=openssl`
  - `http.version=HTTP/1.1`
- Patched `.codex/skills/workspace-submodule-pull-all/scripts/pull_all_submodules.ps1` so a detached HEAD submodule is skipped instead of crashing.

## Tentative

- The user may want the GitHub proxy workaround kept as a repeatable option for future pull operations.
