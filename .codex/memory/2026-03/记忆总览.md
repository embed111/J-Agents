# 2026-03 记忆总览

> 该文件只归档本月“历史日期”的摘要，不收录当天仍在追加中的每日记忆。

## 已归档日期摘要

### 2026-03-14

- 在 `.codex/` 下建立连续性骨架，并明确治理仍以 `AGENTS.md` 为准。
- 记录 GitHub submodule pull 在本工作区的可行条件：进程级代理、`http.sslBackend=openssl`、`http.version=HTTP/1.1`。
- 修复 `workspace-submodule-pull-all`，使 detached HEAD 子仓在拉取时跳过而不是崩溃。

### 2026-03-18

- 把工作区提交流程稳定到“先子仓、后根仓”的 submodule 工作流。
- 增强 `workspace-submodule-commit-push-all`：支持 dirty detached 子仓回到提交分支、缺失提交身份时继承根仓配置、`git push` 自动重试。
- 完成 `workflow`、`Analyst`、`agent_gen` 与根仓的一次全工作区提交和推送。

### 2026-03-19

- 补录：今日已先停掉会持续写入 workflow 运行时目录的相关进程，丢弃本地 runtime 改动后把根仓与递归子仓拉到最新，并把 `.output-test/` 加入根仓忽略；本轮又将 `.codex` 记忆体系重构为“规范文件 + 全局总览 + 月总览 + 每日记忆”，补上开工检查与回合总结脚本，并迁移了 2026-03-14、2026-03-18 的历史记忆。
- 核对了全局记忆总览路径，仓库内没有残留的 `memeory` 错误引用；当前规范、脚本和实际文件均使用 `.codex/memory/全局记忆总览.md`，因此无需再做路径迁移，仅确认按正确路径继续执行。
- 评估了当前记忆维护能力：现状是已有 `ensure_memory_context.ps1` 和 `append_daily_memory.ps1` 两个脚本，但本地技能仍只覆盖 submodule 拉取/提交/兼容旧提交流程，记忆体系缺少技能层封装。结论是建议新增技能，至少补一个“开工读取与检查记忆上下文”技能，以及一个“日切/月切归档记忆”技能；否则流程仍依赖临时自觉执行，稳定性不够。
- 新增了两个本地记忆技能：`workspace-memory-context` 用于开工前读取与校验记忆上下文，`workspace-memory-archive` 用于日切/月切归档；同时补充了 `.codex/scripts/archive_memory.ps1` 作为工作区归档脚本，并把两项技能登记进 `AGENTS.md`。已通过 skill 结构校验，并验证了 context 入口、archive 常规 dry-run 和月切 dry-run。
- 补充修正：`workspace-memory-archive` 初版 dry-run 会意外创建未来月份模板，现已改成纯预演，不再写入文件；同时清理了验证过程中误生成的 `2026-04` 记忆模板，并复核月切 dry-run 不再产生副作用。
- 评估了每日记忆粒度，结论是应当提高到“结构化详细”层级：每日记忆要比月总览和全局总览更细，至少覆盖本轮目标、实际动作、关键文件/脚本、关键决策、验证结果、残留问题与下一步，这样后续做记忆回溯和归档提炼会明显更稳。当前的一句话式总结可用，但更适合作为时间线，不足以支撑高质量回溯。
- 把每日记忆升级为更详细但不强制结构化的写法，并同步调整写入脚本，避免后续记录变成过重的模板填空。
- 这轮主要改了 `.codex/MEMORY.md` 和 `.codex/scripts/append_daily_memory.ps1`，保留了旧的 `-Summary` 单字段调用方式。
- 已验证旧写法仍可追加记录，后续如果需要补更多上下文，可以直接增加几条明细 bullet。
- 历史旧记录暂不回填重写；后续新增记录按“稍微更详细一些”的方式继续写。
- 把每日记忆规则从强模板调整回“自由写但略详细”的形式，重点保留回溯所需信息，不再要求每轮都按固定分段填写。
- 更新了 `.codex/MEMORY.md`，明确每日记录不要求固定结构，但需要比一句话更完整。
- 调整了 `.codex/scripts/append_daily_memory.ps1`，现在默认写成时间戳加多条自然语言 bullet，不再依赖结构化字段。
- 同步把今天那条结构化样例改回普通详细记录，避免当前日期内风格过于割裂。
- 收口了每日记忆写入接口，后续默认只需要写一段稍微详细一些的总结，必要时再补几条明细。
- 这次把脚本参数简化成 `Summary` 和 `Details` 两个文本入口。
- 这样通过 `powershell -File` 调用时不会再因为数组参数展开导致记录错位。
- 总览回溯仍然依赖月度和全局总览按日期、月份做摘要归档。

### 2026-03-20

- 定位了 `npm install -g @openai/codex` 失败的原因，确认是 Windows 上 `codex.exe` 正在被运行中的进程占用，npm 在升级时需要把旧安装目录整体挪走，因此在复制/移动可执行文件时触发 `EBUSY`。
- 检查了 npm 日志，失败点发生在把 `C:\Users\think\AppData\Roaming\npm\node_modules\@openai\codex\...\codex.exe` 复制到临时退休目录时。
- 同时检查到多个来自全局 npm 安装目录的 `codex.exe` 和 `node.exe` 进程仍在运行，这会直接锁住该可执行文件。
- 日志里的 `errno -4082` / `EBUSY` 在 Windows 上就是“资源忙或被锁定”，通常意味着程序仍在运行、被终端占用，或被安全软件短暂扫描。
- 结论是更新前先退出所有正在运行的 Codex 会话和相关 `node` 进程，再重试全局安装；必要时关闭占用该路径的 VS Code/Codex 扩展终端，或重启一个干净 shell 后执行。
- 补齐了本轮工作前的记忆归档检查，先把 2026-03-19 的每日记忆归入 2026-03 月度总览，再继续当前提交任务。
- 按“先子仓、后根仓”的工作流清理并同步了整个工作区，先停掉会持续写入 runtime 的 workflow 相关进程，避免提交过程中又产生脏文件。
- 已将有实际改动的 3 个子仓和根仓全部提交并推送到 GitHub，根仓 gitlink 也已跟到最新子仓提交。
- Analyst 从 detached HEAD 切回 main 后提交并推送到 7b7022a；agent_gen 只纳入明确的 reports 变更以避开 .unit_test/tmp 的权限问题，最终推送到 57a63d4；workflow 清理了超时遗留的 git index.lock 后完成提交并推送到 ea48e24。
- 根仓 J-Agents 提交为 cba9365，首次网络不稳定后改用带重试的 HTTP/1.1 push 成功推送到 origin/main。
- 递归子模块共 8 个，本轮实际有改动并处理的仓库是 Analyst、agent_gen、workflow 和根仓；Analyst2、PortraitRpDemo、openclaw、skill_manager、trainer 因无改动而跳过。
- 结束前复核了根仓、Analyst、agent_gen、workflow 的 git status，均为 main 跟踪 origin/main；根仓和 agent_gen 仍会对 .unit_test/tmp 输出 Permission denied 警告，但不是 Git 变更。

### 2026-03-23

- 确认当前仓库拉取与推送流程都要包含根仓；拉取顺序改为先拉取再保存记忆，推送顺序改为先保存记忆再推送。
- 这轮修改并验证了 .codex/skills/workspace-submodule-pull-all/scripts/pull_all_submodules.ps1 与 .codex/skills/workspace-submodule-commit-push-all/scripts/commit_push_all_submodules.ps1，新增 SaveMemoryAfterPull 与 SaveMemoryBeforeRootCommit 记忆钩子，并保留根仓拉取需显式 IncludeRoot 的治理边界。
- 为减少大仓推送前卡顿，把提交脚本的暂存逻辑从笼统 git add -A 改成 git add -u 加 untracked pathspec 批量加入，重点缓解 workflow 这类高变更仓库的暂存开销。
- 预演验证结果：拉取 dry-run 已覆盖根仓与 8 个递归子仓，推送 dry-run 显示将提交并推送 Analyst、workflow 和根仓；期间修复了 Join-Labels 对空数组报错的问题。
- 当前根仓在 fetch 后为 ahead 1, behind 1，且根仓、Analyst、workflow 仍有本地改动，所以真正执行含根仓 pull 时这些仓会先被技能判定为 skip，需要先处理本地改动。

### 2026-03-24

- 准备提交根仓：submodules=8 committed=2 pushed=0
- 已提交子仓：workflow, Analyst
- 已推送子仓：none
- 未变更子仓：skill_manager, openclaw, trainer, Analyst2, agent_gen, PortraitRpDemo
- 下一步：提交并推送根仓
- 使用技能：workspace-submodule-commit-push-all
- 准备提交根仓：submodules=8 committed=1 pushed=0
- 已提交子仓：workflow
- 未变更子仓：skill_manager, openclaw, trainer, Analyst2, Analyst, agent_gen, PortraitRpDemo
- 完成本轮全工作区提交收口：workflow 与 Analyst 已形成待推送提交，根仓已合并 origin/main 并保留远端删除 PortraitRpDemo 的结果。
- 这轮先修复了 workspace-submodule-commit-push-all 在 workflow 仓卡死的问题：把 tracked 变更暂存从 git add -u 改为按 pathspec 批量加入，并把 Git 子进程 stdout/stderr 改成并发读取，避免 CRLF 警告撑满 stderr 缓冲区导致假死。
- 随后在 2026-03-24 先后完成 workflow 提交 116c810、bb51a9d，Analyst 提交 008c06c，以及根仓提交 afb6ebc、e79d2fa；之后将根仓与 origin/main 合并，手工保留本地 .output-test 记录并合并记忆文件，生成 merge 提交 24f710e。
- 本次 merge 吸收了远端对 PortraitRpDemo 的删除，同时保留本地技能脚本、记忆规则和子仓 gitlink 更新；本地 PortraitRpDemo 目录保留为未跟踪目录，没有被重新加入根仓索引。
- 当前待执行的最后一步是按顺序推送 Analyst、workflow 和根仓；workflow 工作树仍有 3 个与索引 blob 哈希相同但 stat 状态为 modified 的路径，不影响对应已提交历史的推送。

### 2026-03-25

- 完成当前工作区拉取：根仓已确认与 origin/main 同步，递归子仓已更新到远端最新；记忆在拉取完成后补写。
- 这轮按修正后的顺序先执行代码拉取，再处理记忆；根仓 J-Agents 最终状态为 HEAD 与 origin/main 无 ahead/behind，递归子仓中 Analyst、agent_gen、Analyst2、openclaw、skill_manager、trainer、workflow 均已拉取到当前远端提交。
- 中途发现 workspace-submodule-pull-all 有两个实现问题：一是先前执行中我误把记忆归档放到拉取前，导致根仓被 dirty 判定为 skip；二是脚本汇总阶段对异常结果对象不够鲁棒，已在本地技能脚本 .codex/skills/workspace-submodule-pull-all/scripts/pull_all_submodules.ps1 中补强。
- workflow 一度因运行中的 workflow_web_server.py 以及 .running/prod 下被 skip-worktree 隐藏的已跟踪 __pycache__ 文件而无法 ff-only pull；这轮已先停掉本地进程，再用 git restore --ignore-skip-worktree-bits 清掉阻塞文件，随后成功从 bb51a9d 快进到 c7cd3ad。
- 当前根仓仍显示本地修改，来源是本轮子仓 gitlink 更新到 Analyst=a874a97、agent_gen=0b5db2b、workflow=c7cd3ad，以及本地 pull 技能脚本修复；这些都是拉取后的正常结果或本轮技能修复，不是新的拉取失败。
- 按用户要求在 .codex 下新增可复用经验目录，用于持续沉淀拉取/推送与排障经验，减少重复踩坑。
- 这轮把反复踩坑的原因收敛为几类：submodule 与根仓顺序要求严格、workflow 这类仓包含会持续写入的运行时文件、Windows 下行尾与 skip-worktree 会制造隐藏 dirty、以及本地技能脚本自身也在演进。
- 已新增 .codex/experience/README.md 作为维护约定，并新增 .codex/experience/git-submodule-pull-push.md 记录当前已验证的拉取/推送经验、触发条件、避免方式与恢复步骤。
- 同时把该约定写入 AGENTS.md 和 .codex/MEMORY.md，后续只要遇到可避免踩坑或值得复用的经验，就优先更新 .codex/experience/ 下对应主题文件。

### 2026-03-26

- 准备提交根仓：submodules=7 committed=2 pushed=0
- 已提交子仓：workflow, Analyst
- 已推送子仓：none
- 未变更子仓：skill_manager, trainer, openclaw, Analyst2, agent_gen
- 下一步：提交并推送根仓
- 使用技能：workspace-submodule-commit-push-all
- 完成最新代码拉取：根仓与 7 个递归子仓已同步，并修复 pull 技能的 dirty 误判
- 开工前按工作区记忆规范读取 .codex/MEMORY.md、全局总览、2026-03 月度总览和 2026-03-26 每日记忆；期间检测到昨日 2026-03-25 尚未归档，已先归档到月度总览。
- 首次执行 workspace-submodule-pull-all 时，脚本把 git status --porcelain 的 stderr 权限警告也计入了脏状态，导致根仓和 agent_gen 被误判为 skipped-dirty；其余子仓中 Analyst、Analyst2、openclaw、skill_manager、trainer、workflow 成功拉取，openclaw 经过 1 次重试后成功。
- 随后单独对根仓执行 fetch 和 pull，确认 origin/main 从 cff9ac2 前进到 8a5baa0；第一次 pull 因 GitHub 443 连接失败中断，第二次重试成功 fast-forward。
- 本次根仓更新已吸收远端提交中的 .codex/experience/ 经验目录、.codex/MEMORY.md 与月度/每日记忆更新，以及 Analyst=fcd753e、workflow=d8bf77e 的 gitlink 变化；当前 7 个递归子仓都与根仓记录一致。
- 为避免后续再把 .unit_test/tmp 的 Permission denied 警告误判成本地改动，已在 .codex/skills/workspace-submodule-pull-all/scripts/pull_all_submodules.ps1 中把 stdout 与 stderr 分离，并让 dirty 检查只看 stdout 状态输出。
- 这轮曾临时创建 codex-pre-pull-memory-2026-03-26 与 codex-root-pull-recovery-2026-03-26 两个 stash；其中月度总览内容已被远端更完整版本覆盖，脚本修复与本轮结果已单独恢复，因此后续可安全清理这两个临时 stash。
- 使用技能：workspace-memory-context、workspace-memory-archive、workspace-submodule-pull-all。
- 更新拉取流程约束：不要因 workflow/.running/prod 主动停进程
- - 用户明确要求：后续执行拉取代码时，不要因为 workflow/.running/prod 的运行进程而主动停止服务；该目录默认不应视为 Git 跟踪内容。
- - 已把这条约束写入 .codex/MEMORY.md 的已确认偏好，作为稳定工作规则保留。
- - 同时更新 .codex/experience/git-submodule-pull-push.md：拉取阶段默认不主动杀 workflow/.running/prod 相关进程，只有在 Git 明确报出已跟踪运行时文件阻塞 pull --ff-only 时，才进入恢复处理。
- - 这轮只更新了连续性文档，没有执行 pull、commit 或 push。
- 提取 trainer AGENTS 角色策略 JSON
- 按工作区要求先读取 .codex/MEMORY.md、全局总览、2026-03 月度总览和当日记忆。
- 读取 C:/work/J-Agents/trainer/AGENTS.md，仅基于其内容抽取 role、goal、must、must_not、preconditions，并为每条结论保留原文证据。
- 识别到 trainer AGENTS 的核心约束包括严格单轮流程、启动读取顺序、候选体文件修改边界、高风险任务交付边界，以及日志必须落到 .training 而非 .codex/MEMORY.md。
- 本轮输出不做代码修改，只生成结构化 JSON；另标记该 AGENTS 同时定义训练师整体角色与多个子角色，角色粒度存在轻微歧义。
- 读取并校验工作区记忆上下文后，读取 C:/work/J-Agents/trainer/AGENTS.md。
- 仅基于该 AGENTS.md 抽取 role、goal、responsibilities.must、responsibilities.must_not、responsibilities.preconditions，并为每条结论保留原文 evidence。
- 识别到该文件同时定义训练师整体职责与多个子角色，role 粒度存在一定歧义，因此在 warnings 中标注角色范围需确认。
- 本轮不修改业务代码，只输出单个 JSON 对象。
- 复核 trainer/AGENTS.md 的段落与行号后，整理最终结构化提取结果。
- role 采用“训练师工作区（含 trainer_intake、trainer_judge、trainer_audit、analyst_prod、analyst_cand）”的聚合表达，并在 warnings 中保留多角色文档歧义提示。
- goal 汇总自“目标”三条原文；must、must_not、preconditions 分别锚定到“单轮流程（严格顺序）”“关键约束”“启动读取顺序/关键约束/输出规范”。
- 本轮仅输出 JSON，没有修改 trainer 工作区业务文件。
- 识别到该文件同时定义训练师整体目标与多个子角色，role 粒度存在一定歧义，因此在 warnings 中标注需确认。
- 确认卸载 ccman 不会影响本机 codex CLI 运行
- - 本机全局 npm 当前同时安装了 @openai/codex@0.116.0 与 ccman@3.3.19。
- - 已检查启动脚本：codex.ps1 指向 C:\Users\think\AppData\Roaming\npm\node_modules\@openai\codex\bin\codex.js，ccman.ps1 指向 C:\Users\think\AppData\Roaming\npm\node_modules\ccman\dist\index.js，二者是独立入口。
- - 已检查 @openai/codex 的 package.json，只有自身平台包 optionalDependencies，没有声明依赖 ccman。
- - 已执行 codex --version，当前可正常返回 codex-cli 0.116.0。
- - 结论：卸载 ccman 只会移除 ccman 命令；只要不依赖它来代管配置或启动包装，codex 本体仍能继续运行。
- 梳理本机 ccman 的定位与主要能力
- - 基于本机已安装的 ccman@3.3.19 README 和 --help 输出，整理了它的作用、支持对象和常用命令。
- - 结论：ccman 不是模型本体，而是多工具的配置管理器，核心价值是统一切换不同 API 服务商、维护 MCP 服务器配置，以及做导入导出和可选的 WebDAV 同步。
- - 本机 help 已确认支持 Codex、Claude Code、Gemini CLI、OpenCode、OpenClaw、MCP，并提供 gmn/gmn1 快捷配置命令。
- - 这轮只做环境说明，没有修改代码或执行拉取推送。

### 2026-03-29

- 按治理要求先读取 `.codex/MEMORY.md`、全局记忆总览、2026-03 月度总览和当日记忆，再处理用户指定的 `workflow_tester/AGENTS.md`。
- 基于目标 `AGENTS.md` 提取了 `workflow_tester` 的角色、目标、必须事项、禁止事项与前置条件，证据仅取自该文件的 `Identity`、`Portrait`、`Collaboration` 与 `Startup Read Order` 段落。
- 解析中发现目标文件没有显式 `goal` 标题，最终以 `Role` 与 `capability_summary` 的一致表述作为目标依据，并在输出中保留该信息。
