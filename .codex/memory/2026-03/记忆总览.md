# 2026-03 记忆总览

> 该文件只归档本月“历史日期”的摘要，不收录当天仍在追加中的每日记忆。

## 已归档日期摘要

### 2026-03-14

- 在 `.codex/` 下建立连续性骨架，并明确治理仍以 `AGENTS.md` 为准。
- 记录 GitHub submodule pull 在本工作区的可行条件：进程级代理、`http.sslBackend=openssl`、`http.version=HTTP/1.1`。
- 修复 `workspace-submodule-pull-all`，使 detached HEAD 子仓在拉取时跳过而不是崩溃。

### 2026-03-18

- 把工作区提交流程稳定到“先子仓、后根仓”的 submodule 工作流。
- 增强 `workspace-submodule-commit-push-all`：支持 dirty detached 子仓回到提交分支、缺失提交身份时继承根仓配置、`git push` 自动重试。
- 完成 `workflow`、`Analyst`、`agent_gen` 与根仓的一次全工作区提交和推送。

### 2026-03-19

- 补录：今日已先停掉会持续写入 workflow 运行时目录的相关进程，丢弃本地 runtime 改动后把根仓与递归子仓拉到最新，并把 `.output-test/` 加入根仓忽略；本轮又将 `.codex` 记忆体系重构为“规范文件 + 全局总览 + 月总览 + 每日记忆”，补上开工检查与回合总结脚本，并迁移了 2026-03-14、2026-03-18 的历史记忆。
- 核对了全局记忆总览路径，仓库内没有残留的 `memeory` 错误引用；当前规范、脚本和实际文件均使用 `.codex/memory/全局记忆总览.md`，因此无需再做路径迁移，仅确认按正确路径继续执行。
- 评估了当前记忆维护能力：现状是已有 `ensure_memory_context.ps1` 和 `append_daily_memory.ps1` 两个脚本，但本地技能仍只覆盖 submodule 拉取/提交/兼容旧提交流程，记忆体系缺少技能层封装。结论是建议新增技能，至少补一个“开工读取与检查记忆上下文”技能，以及一个“日切/月切归档记忆”技能；否则流程仍依赖临时自觉执行，稳定性不够。
- 新增了两个本地记忆技能：`workspace-memory-context` 用于开工前读取与校验记忆上下文，`workspace-memory-archive` 用于日切/月切归档；同时补充了 `.codex/scripts/archive_memory.ps1` 作为工作区归档脚本，并把两项技能登记进 `AGENTS.md`。已通过 skill 结构校验，并验证了 context 入口、archive 常规 dry-run 和月切 dry-run。
- 补充修正：`workspace-memory-archive` 初版 dry-run 会意外创建未来月份模板，现已改成纯预演，不再写入文件；同时清理了验证过程中误生成的 `2026-04` 记忆模板，并复核月切 dry-run 不再产生副作用。
- 评估了每日记忆粒度，结论是应当提高到“结构化详细”层级：每日记忆要比月总览和全局总览更细，至少覆盖本轮目标、实际动作、关键文件/脚本、关键决策、验证结果、残留问题与下一步，这样后续做记忆回溯和归档提炼会明显更稳。当前的一句话式总结可用，但更适合作为时间线，不足以支撑高质量回溯。
- 把每日记忆升级为更详细但不强制结构化的写法，并同步调整写入脚本，避免后续记录变成过重的模板填空。
- 这轮主要改了 `.codex/MEMORY.md` 和 `.codex/scripts/append_daily_memory.ps1`，保留了旧的 `-Summary` 单字段调用方式。
- 已验证旧写法仍可追加记录，后续如果需要补更多上下文，可以直接增加几条明细 bullet。
- 历史旧记录暂不回填重写；后续新增记录按“稍微更详细一些”的方式继续写。
- 把每日记忆规则从强模板调整回“自由写但略详细”的形式，重点保留回溯所需信息，不再要求每轮都按固定分段填写。
- 更新了 `.codex/MEMORY.md`，明确每日记录不要求固定结构，但需要比一句话更完整。
- 调整了 `.codex/scripts/append_daily_memory.ps1`，现在默认写成时间戳加多条自然语言 bullet，不再依赖结构化字段。
- 同步把今天那条结构化样例改回普通详细记录，避免当前日期内风格过于割裂。
- 收口了每日记忆写入接口，后续默认只需要写一段稍微详细一些的总结，必要时再补几条明细。
- 这次把脚本参数简化成 `Summary` 和 `Details` 两个文本入口。
- 这样通过 `powershell -File` 调用时不会再因为数组参数展开导致记录错位。
- 总览回溯仍然依赖月度和全局总览按日期、月份做摘要归档。

### 2026-03-23

- 确认当前仓库拉取与推送流程都要包含根仓；拉取顺序改为先拉取再保存记忆，推送顺序改为先保存记忆再推送。
- 这轮修改并验证了 .codex/skills/workspace-submodule-pull-all/scripts/pull_all_submodules.ps1 与 .codex/skills/workspace-submodule-commit-push-all/scripts/commit_push_all_submodules.ps1，新增 SaveMemoryAfterPull 与 SaveMemoryBeforeRootCommit 记忆钩子，并保留根仓拉取需显式 IncludeRoot 的治理边界。
- 为减少大仓推送前卡顿，把提交脚本的暂存逻辑从笼统 git add -A 改成 git add -u 加 untracked pathspec 批量加入，重点缓解 workflow 这类高变更仓库的暂存开销。
- 预演验证结果：拉取 dry-run 已覆盖根仓与 8 个递归子仓，推送 dry-run 显示将提交并推送 Analyst、workflow 和根仓；期间修复了 Join-Labels 对空数组报错的问题。
- 当前根仓在 fetch 后为 ahead 1, behind 1，且根仓、Analyst、workflow 仍有本地改动，所以真正执行含根仓 pull 时这些仓会先被技能判定为 skip，需要先处理本地改动。
