# 2026-03-20 每日记忆

## 工作记录

### [2026-03-20 17:18:33 +08:00]

- 定位了 `npm install -g @openai/codex` 失败的原因，确认是 Windows 上 `codex.exe` 正在被运行中的进程占用，npm 在升级时需要把旧安装目录整体挪走，因此在复制/移动可执行文件时触发 `EBUSY`。
- 检查了 npm 日志，失败点发生在把 `C:\Users\think\AppData\Roaming\npm\node_modules\@openai\codex\...\codex.exe` 复制到临时退休目录时。
- 同时检查到多个来自全局 npm 安装目录的 `codex.exe` 和 `node.exe` 进程仍在运行，这会直接锁住该可执行文件。
- 日志里的 `errno -4082` / `EBUSY` 在 Windows 上就是“资源忙或被锁定”，通常意味着程序仍在运行、被终端占用，或被安全软件短暂扫描。
- 结论是更新前先退出所有正在运行的 Codex 会话和相关 `node` 进程，再重试全局安装；必要时关闭占用该路径的 VS Code/Codex 扩展终端，或重启一个干净 shell 后执行。
