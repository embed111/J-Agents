# 2026-04-05 每日记忆

## 工作记录

### [2026-04-05 00:53:13 +08:00]

- 查询 ChatGPT Plus 在 Codex 上的额度口径，确认官方不公布固定每周 credit 数
- 本轮按 OpenAI 官方帮助中心核查 ChatGPT Plus 的 Codex 使用口径。结论：截至 2026-04-05，OpenAI 对 Plus 的 Codex 不公布一个固定“每周多少 credits”的公开数字；官方说法是 Plus 先消耗 plan included usage，超出后才会开始消耗单独购买的 credits。帮助中心当前公开口径强调：Codex 限额取决于 plan、任务复杂度、以及本地/云端执行方式；同时存在 5 小时窗口和共享周限额。Codex rate card 仅提供平均 credits 成本，不代表 Plus 自带 credits 数。可给用户的实用换算是：本地任务平均约 GPT-5.4=7 credits/条、GPT-5.3-Codex=5 credits/条、GPT-5.1-Codex-mini=1 credit/条；云端任务平均约 GPT-5.4=34 credits/条、GPT-5.3-Codex=25 credits/条。若用户想看自己的真实剩余额度，应进入 Codex Settings -> Usage Dashboard 查看，而不是按公开文档反推一个固定周额度。

### [2026-04-05 00:57:54 +08:00]

- 评估 CSDN 关于 Codex stream disconnected 的排障文，结论为部分有参考性但不完全适配当前机器
- 本轮阅读并评估 CSDN 文章 https://blog.csdn.net/qq_34050399/article/details/158851534 ，并与 OpenAI 官方帮助文档和此前本机定位结果对照。结论：文章的分析路径在“Windows Schannel 报 SEC_E_UNTRUSTED_ROOT / TLS 证书链异常”这一类症状上有明显参考价值，尤其是先用 curl 复现、再看浏览器证书、再区分 TLS 错误和 Cloudflare 403 的思路是合理的；但其核心修复动作 certutil 刷新根证书只适用于根证书库异常，不应直接泛化到所有 stream disconnected。对当前机器而言，之前已确认 chatgpt.com 直连失败、经 127.0.0.1:7890 代理可达、Clash 规则缺少 OpenAI 专门规则、Codex CLI 启动环境没有 HTTP_PROXY/HTTPS_PROXY，因此当前更像代理链路或分流问题，而不是已证实的系统根证书损坏。后续对用户应给出“有条件参考”的结论：若本机能稳定复现 schannel/SEC_E_UNTRUSTED_ROOT，再考虑证书修复；否则优先处理代理和规则问题。

### [2026-04-05 19:38:13 +08:00]

- 排查 workflow_code 未被拉取原因
- - 本轮按工作区要求先执行 workspace-memory-context，确认 .codex/MEMORY.md、全局记忆总览、月度总览与当日记忆均已就绪，无待归档告警。
- - 核对根仓 .gitmodules 与 workspace-submodule-pull-all 脚本后确认：当前递归受管 submodule 共 7 个，workflow 相关只有 `workflow`，没有 `workflow_code`。
- - workflow/AGENTS.md 与 workflow/协作约定.md 明确把 `../workflow_code` 定义为独立的 code-only 正式代码根仓，不属于当前 PM 壳仓 `workflow` 本身。
- - 实际磁盘上 `D:\code\AI\J-Agents\workflow_code` 不存在；在 D:\code\AI 范围内检索到的唯一 `workflow_code` 仅是 `workflow\.test\runtime\workflow-gate\workspace-root\workflow_code` 下的测试夹具仓。
- - 标准拉取脚本只遍历 `git submodule status --recursive` 返回的仓库，因此不会去拉不存在于 .gitmodules 中的 `workflow_code`；dry-run 同时显示 `workflow` 因本地改动被 `[SKIP] workflow has local changes.` 跳过。
- - 结论：上次没拉到的根因不是脚本漏掉一个已登记子仓，而是当前工作区没有被纳入受管范围的真实 `workflow_code` 仓；如果你要拉它，需要先把真实仓放到约定路径或显式把外部路径接入流程。

### [2026-04-05 19:42:43 +08:00]

- 将 workflow_code 从 GitHub 接入为根仓 submodule
- - 本轮先通过 `git ls-remote https://github.com/embed111/workflow_code.git HEAD` 与 `refs/heads/main` 确认远端仓库可访问，默认使用 `main`。
- - 随后在根仓执行 `git -c http.version=HTTP/1.1 submodule add -b main https://github.com/embed111/workflow_code.git workflow_code`，成功把 `workflow_code` 克隆到 `D:\code\AI\J-Agents\workflow_code`，并写入 `.gitmodules`。
- - 当前 `.gitmodules` 已新增 `workflow_code` 条目（path=`workflow_code`、url=`https://github.com/embed111/workflow_code.git`、branch=`main`），根仓 `git status` 显示 `.gitmodules` 修改且 `workflow_code` 作为新增 gitlink 待提交。
- - 验证结果：`git submodule status --recursive` 已显示 `workflow_code` 在 commit `8343a2f5ef1dcc84f77780e3fa55076d685e1d24`、分支 `main`；标准技能 dry-run 显示递归 submodule 数量从 7 变为 8，并会对 `workflow_code` 执行 `fetch + pull origin/main`。
- - 本轮未执行根仓提交/推送；`workflow` 仍因本地改动在标准拉取脚本里会被跳过，这个状态未被改动。
